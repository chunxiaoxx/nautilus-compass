# 735 cohort 切片 · 探源报告(2026-09-24 晚 · 降险预探)

> 目标:9/25 21:00 前向 flywheel 直链交付 NanoJev/GENESIS episode 级切片
> (v0.3 §E 字段集)+ subject 指纹口径(承函 735,ack=936,flywheel 回执 940)。
> 本档=数据源坐标+字段映射+执行方案,明日按此施工。

## 数据源(已实测)

- 仓:`TianyuCodings/NanoJev`(浅克隆于 Temp/nanojev-t3,**core.autocrlf=false**
  ——Genesis #2 教训:CRLF 转换毁哈希)
- **帧级本体**:`web/side_by_side_results.json` → `examples[*].systems[*].frames`
  - 2 局(maze 50×50 / snake 12×12)× 3 系统(nanojev/jev/base)= 6 episodes
  - maze 帧数:nanojev 245 / jev 2739 / base 4727(帧数=steps+1 ✓)
  - 帧字段:`position[x,y] / score / action / probabilities / collision /
    decision_source / done`
- cohort 定义锚(初始状态+冻结口径):`results/rollout_pilot_episodes.jsonl`(6 条)
  `results/side_by_side_maze_episode.jsonl`(1 条,含 source_cohort_sha256)
  `results/arcade_snake_cohort.jsonl`(8 条)+ `research/nanojev_comparison_cohort.json`
- 回放工具(如需):`scripts/replay_unified_episodes.py`(纯 CPU 模拟器,
  重放已记录 actions,零模型调用,1e-9 容差)

## §E 字段映射(设计)

| §E 要求 | 来源 | 处理 |
|---|---|---|
| `frames` 数组 | systems[*].frames | 提取+注入元字段 |
| `episode_id` | 注入 `<game>-<system>`(如 maze-nanojev) | episode_by 分组键 |
| `t` | 帧序号 0..N-1 | incr_eq 1 不变量 |
| 运动学 | position[x,y] | 格子运动:每轴 abs_delta_le 1 |
| 帧级有效性 | score ge_const 0;t ge_const 0 | |
| collisions | collision 字段 | §E 分工:终局计数走 aggregate(count),不进 episode 不变量 |

N=6 episodes(2 局×3 系统,含对照组——给 flywheel 8 算子留空间);
flywheel 口径"N 条量级即可"✓。

## subject 指纹口径(随切片同函,沿 #635 §R「改脑即新主体」)

三元组=①NanoJev 仓 commit sha ②SOURCE_MANIFEST.json sha256(全文件清单)
③frames 源文件(web/side_by_side_results.json)sha256——**LF 原始字节口径**
(注明提取脚本与转换规则,第三方可复算)。

## 直链交付形态

切片文件:`runtime/verifypack/cohort_nanojev_v1/`(frames.json+manifest.json
含三元组+提取脚本),GitHub raw 直链入函;**入库前本地跑 tools/verifypack
episode check 自检**(§E:帧缺字段→CheckError,先自绿再交)。

## 风险与预案

- 哈希口径:全链 LF;Windows 落盘显式 newline="\n"
- snake 局 position 可能含生长语义(多节蛇?frame 只有 position 单点——
  若 snake 是单点表示则直接可用;施工时验证,异常则 maze-only 降级+口径注明)
- 自检不过:先证伪自己(字段映射/口径),再降级交付

## 明日清单(9/25)

1. 提取脚本+frames.json+manifest(30min)2. verifypack episode 自检(30min)
3. commit+raw 直链 4. 交付函(含指纹口径)→ flywheel
卡点哨:12:00 前报(承 936 承诺)。
