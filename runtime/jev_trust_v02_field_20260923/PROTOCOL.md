# jev-trust v0.2 双极性实测协议(预注册 · 2026-09-23)

> 功能实测:decide_symmetric 在域 4 真数据上的冲突捕获率。
> 这是功能验证非新研究,但读数可能对外(Discord follow-up),故按纪律
> 落档:开工写死样本与口径,不设好坏门,如实报。

## 样本

> ⚠️ 修正记录(开工前,零 API 调用发生时):初版写「检出 6+7」系凭
> 印象错误——域 4 实际 spike 检出仅 1、stuck 仅 2(漏检 14/13)。
> 检出题不足以构成对照组,改为下述设计。

从域 3/域 4 会话分层取 **50 题**:
- **A 组 spike 缺陷 15 题**(域 4 漏 14 + 检出 1)
- **B 组 stuck 缺陷 15 题**(域 4 漏 13 + 检出 2)
- **C 组 clean 20 题**(域 4 全对——「模型会做」对照组;取前 20)
A+B=「算不动」难组(29 漏/3 检),C=会做组;冲突率按组读。

## 调用

- 每题一次 decide_symmetric(一调用含正反两问)
- 正向问法=域 3 spec(NEEDS_REVIEW),反向问法=域 4 spec(OK)
  ——即已实证过的两种极性原文,不自创新措辞
- jev-latest,内容零重试

## 口径(写死)

- **冲突率**:polarity_consistent=False 比例,总体+分组(漏检组 vs 检出组)
- 预期观察项(非门):若「漏检组冲突率 > 检出组冲突率」→ 双极性自检
  与真实失效正相关(功能有效信号);若两组无差 → 自检信号与该失效
  正交(如实报,功能价值待重估)
- 双极性判定本身的正确率:consistent 时 mapped decision 与真值比对
- 全量工件落账(decision_set 引用域 3 原件+抽样清单+session.jsonl+sig)

## 收工

RESULTS.md:冲突率总体/分组+判定正确率+边界;**待非实现者复算**。
