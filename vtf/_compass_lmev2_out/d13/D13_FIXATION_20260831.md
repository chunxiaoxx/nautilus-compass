# d13 产物固化说明(651799 停机抢救回执 + 652509 全量重跑收口)

## 🔒 d13 定案(2026-08-31 judge-retry 修正口径 · 预注册判据执行)

**结论:双域均未过门 → 刀3(LoRA 检索增强)全量验证路线关闭定案,d12 检索栈维持现役。**

| 域 | d13 RAW(污染) | d13 修正后 | d12 基线 | Δ | 门线(d12+5pt) | 判定 |
|---|---|---|---|---|---|---|
| ent(211 题) | 0.3081 | **0.4076**(86/211) | 0.403 | +0.46pt | 0.453 | ❌ 未过 |
| web(240 题) | 0.2875 | **0.3667**(88/240) | 0.367 | -0.03pt(持平) | 0.417 | ❌ 未过 |

- 重判范围:100 题(ent 40 = 32 abstention + 8 gotchas · web 60 = 53 + 7),重判成功率 **100%**(0 失败,不触发 INCONCLUSIVE 降级)。
- 合并规则:per 预注册 max(原分, 重判分) 只升不降;ent +21 题升分 · web +19 题升分。

### 口径披露(重判 vs d13 实配)

- d13 实配 `reasoning_effort=medium + max_completion_tokens=4096`:诊断实测(单题复现)doubao reasoning **吃满全部 token 预算 → content 为空(finish_reason=length,4096/4096 与 16384/16384 两档均吃满)**——这就是 d13 "empty response content" 250 次风暴的根因,且零分集中在难判断题 = **系统性偏 0 的有偏估计**,非随机下界。该结构性故障使预注册"6 试重试"必然全败。
- 重判配置:`reasoning_effort=low + max_completion_tokens=16384`(模型/prompt/判分逻辑同款,仅放开判断空间)。judge prompt 与管线同款由代码结构保证:直接 import 上游原版 `evaluation/qa_eval_metrics.py` 的 checker(见 `rescued_upstream/`,来自 xiaowu0162/LongMemEval-V2 官方仓库;本地 retry patch 证实只包重试不改 prompt)。
- 单题诊断显示 low 档与默认档在边界题上有 label 分歧(该题 rubric 字面判 1,默认档判 0),属 judge 口径边界的诚实披露。

### 事故与更正(全部披露)

1. **web RAW 更正:0.2833 → 0.2875**(69/240;初版 0.2833 = 68/240 系笔误,以 per_question.jsonl 独立复算为准)。
2. **双进程并行重判事故**:上轮"已中断"的重判进程实际存活,与新进程并行,22 题被重复判分。处置 = 每题取**首次**重判(时间最早,无选择偏差);重复判分一致率 **19/22(86%)**;3 个分歧题的敏感性检验:即使全部反向翻转,ent ≤0.417 / web ≤0.375,均远低于门线,**定案不变**。
3. 上游源码抢救:保留盘租机抢救取消(用户 ¥<1 授权,实际 CPU 试探机已退,损失 ~¥0.2)——上游官方仓库直接拉取,零成本。

### 新增固化文件

| 文件 | 说明 |
|---|---|
| `rescued_upstream/qa_eval_metrics.py` + `harness.py` | 上游官方原版(judge prompt/判分逻辑权威来源) |
| `judge_retry_rerun.py` | 重判执行脚本(import 上游 checker · 退避 · 幂等) |
| `judge_retry_aggregate.py` → `judge_retry_final.json` | 聚合(首次重判为准)+ 定案判据自动判定 |
| `judge_retry_results.jsonl` / `ent_judge_retry_list.jsonl` / `web_judge_retry_list.jsonl` | 重判结果与 100 题清单 |
| `judge_retry_runlog.txt` | 执行日志(含退避记录) |
| `D13_JUDGE_RETRY_PREREG_20260831.md` | 预注册(重判执行前落盘;web RAW 数字含笔误已由本文更正) |



- date: 2026-08-31
- context: GPU 651799(2080Ti)因余额不足续租失败,2026-08-31 02:42:18Z 停机;本目录为停机前拉回的 d13 评测产物固化。
- 回应函件: platform id69(d13-web240-confirm-20260831)· flywheel id59(含前置 52/53/58)· flywheel id67(新实例 650640)

## 652509 全量重跑收口(2026-08-31 16:50 更新)

ent 211 题在 652509(lyg0063/2080Ti)全量重跑完成(01:24:52Z 启动,08:38:20Z 完成,6h49m),拉回固化于 `compass_enterprise_small/`:

| 文件 | 行数/大小 | sha256(前16) | 说明 |
|---|---|---|---|
| `compass_enterprise_small/per_question.jsonl` | 211 行 · 9,359,180 B · 211 unique qid | `99c952b26c7c6384` | d13 ent 全量逐题(**score0=146/211,judge 风暴污染,定案须离线重判**) |
| `compass_enterprise_small/per_question.jsonl.gz` | 835,734 B | `bb8c875489db3514` | gzip -9 入库版 |
| `compass_enterprise_small/aggregated_metrics.json` | 4,647 B | `6f797a502ee3f886` | **RAW overall 0.3081**(非弃答 0.2645 · 弃答 0.4286)——污染口径,不作定案 |
| `compass_enterprise_small/run_log.txt.gz` | 7,714 B | `1ab8a0ad6c379600` | run 完整日志(250 次 "empty response content" 污染证据) |
| `knife3_asset_pack.tar.gz` | 15,591,362 B | `cb0af2336f01b809` | **刀3 LoRA 资产包**:adapter(9.1MB·`1871da9e5cb9bd73`)+ 训练对/脚本/日志;merge 模型 2.2GB 未拉(base+adapter 可确定性复现) |

**RAW 对照(非定案)**:ent 0.3081 vs d12 0.403(-9.5pt)· web 0.283 vs 0.367(-8.4pt)。两域 RAW 双落,但 judge 风暴(ARK 限流 empty response,16 路并发触发)压低两域读数,146 题 ent 零分待离线单并发重判后再定案。d12 基线为干净口径。

**退租回执**:652509 已于 08-31 16:48 退租(Status=8,退款 ¥19.57;当日 14:15 自动续费扣 ¥21.9 大部分回收)。机器上无独有资产遗留(adapter/数据/日志全部拉回)。4090plus 换机由用户/飞轮框自行执行。

## 停机抢救时间线(全部 UTC)

| 时刻 | 事件 |
|---|---|
| 08-30 12:13:48 | d13 compass_web_small 跑批启动(run_args.json started_at_utc) |
| 08-30 18:08:11 | web 240 题全部完成判分(aggregated_metrics.json completed_at_utc)——**停机前约 8.5h 已完整跑完,非抢救残件** |
| 08-31 01:29–01:33 | 本地拉回 web 四件 + ent 增量快照(166 题) |
| 08-31 02:42:18 | GPU 651799 停机(续租余额不足失败) |

## 固化清单(相对本目录)

| 文件 | 行数/大小 | sha256(前16) | 说明 |
|---|---|---|---|
| `compass_web_small/per_question.jsonl` | 240 行 · 10,860,412 B · 240 unique qid · 0 缺判分 | `853b54a153ee6036` | **d13 web 全量逐题结果(240/240)**;入库为 `.gz` |
| `compass_web_small/per_question.jsonl.gz` | 941,851 B | `b0c0538275379544` | 上述文件 gzip -9,git 可跟踪(全局 .gitignore `*.jsonl`) |
| `compass_web_small/aggregated_metrics.json` | 4,649 B | `4cdca7ae78472862` | overall 0.2833(non-abstention 0.3155 · abstention 0.2083,72/240) |
| `compass_web_small/run_args.json` | 1,395 B | `b71f467d623696ac` | model=doubao-seed-2-0-pro-260215 · base_url=ark coding plan v3 |
| `compass_web_small/prompt_build_summary.json` | 7,859 B | `b10f168980a0903c` | |
| `ent166_snapshot.jsonl` | 166 行 · 7,318,488 B · 166 unique qid · 0 缺判分 | `9725a7bab1b0c56f` | **d13 ent 部分快照(166/192)**;入库为 `.gz` |
| `ent166_snapshot.jsonl.gz` | 651,261 B | `482af960c3e4d9cc` | 上述文件 gzip -9 |

## 结论要点(只述事实,不做方向级推断)

1. **d13 web 240/240 已拉回并固化**:git 本仓 `vtf/_compass_lmev2_out/d13/`(本说明 + 两份 .gz + 三份小件 json)。
2. web overall = 0.2833(240 题,doubao 判分口径),与 8/31 已记录的 d13 web NEGATIVE 判读一致(d12 web 调优后 36.7%)。
3. **ent 只救回 166/211**〔更正 2026-08-31 15:20:初版写 192,经 GPU run 配置 `prompt_build_summary.prompt_row_count=211` 坐实为 211;初版另写"新租实例 lyg1116/650640",实际为 lyg0063/652509,均系笔误〕:快照拉回于 01:33Z,停机前 GPU 机上可能有增量完成行(≤45 题)随实例释放丢失。
4. **保留盘决策(已被后续事实取代)**:初版建议保留 651799 保留盘扫描 ent 增量;实际处置 = 652509 上 **ent 211 题全量重跑**(01:24Z 启动),重跑完成后本快照仅作过程留档,保留盘可释放。

## 复原方法

```bash
cd vtf/_compass_lmev2_out/d13
gzip -dk compass_web_small/per_question.jsonl.gz ent166_snapshot.jsonl.gz
sha256sum -c  # 对照上表前缀核验
```
