# 🪓 compass 减法清单 · 2026-07-07(SSOT §四 compass 角色③)

> 反 D 维护陷阱(ship 件数 ≠ 价值)。**只列不删,砍板留给用户。** 全部判定 grounded:
> 信号 = `git log -1`(last-commit)+ `git grep` 全 repo 引用计数 + `.sh/.service` cron 挂载 + SSOT §七 Parking Lot。
> 基准日 2026-07-07(30 天线=06-06)。compass 抽验 3 个"死"判定(apply_p0_shadow/patch_keepalive/smoke_mcp_full 均 last<06-06 且 0 引用)+ 1 个"活"对照(auto_surface_hook 13 引用)→ 方法可靠。

## 总数(脚本 73:ops 56 + scripts 17)

| 判定 | 数量 | 含义 |
|---|---|---|
| 活 | 33 | 14 天内有 commit 或被活脚本/cron 引用 · 保留 |
| 冻结 | 22 | GPU/A800 依赖(8)+ paper 基准(7)+ spent 一次性件(7)· 留待解冻或人工确认 |
| **死** | **18** | >30d 无 commit **且** 全 repo 零引用 · **砍候选** |

## 🪓 建议砍清单(18 项 · 全是 ship-once-run-once 堆积)

**ops/(15)** —— 8 个 `patch_` + 5 个 `smoke_` + 2 个 `notify_`,均 spent(修复/通告/验证已完成,脚本弃用):
```
ops/apply_p0_shadow.py              P0 shadow 遥测一次性研究件(study 已结)
ops/notify_v7_conn_close_shipped.py 一次性 ship 通告(消息已发)
ops/notify_v7_keepalive_fix.py      一次性通告
ops/patch_cloud_daemon_fallback.py  spent · BGE fallback 已进生产
ops/patch_compass_health.py         spent · /compass/health 已上线
ops/patch_connection_close_v14.py   spent · 头已加
ops/patch_health_probe_strict.py    spent · 已生效
ops/patch_keepalive.py              spent · keep-alive 75 已进 service
ops/patch_v7_daemon_f4_f5_f6.py     spent · V7 daemon 已 ship
ops/smoke_local_recall.py           一次性 smoke(验完即弃)
ops/smoke_mcp_full.py               一次性 MCP smoke
ops/smoke_mcp_handshake.py          一次性 MCP smoke
ops/smoke_mcp_idle_3min.py          一次性心跳 smoke
ops/test_self_audit_v11.py          v1.1 一次性校准(版本已过)
ops/v14_ingest_obs_session_md_fix.py spent · 修复已进生产
```
**scripts/(3)**:
```
scripts/gmail_create_drafts.py  outreach 一次性建草稿(超 compass turf)
scripts/ingest_anchors.py       anchor 一次性入库(已被 live ingest 取代)
scripts/v7_monitor_cron.py      V7 平台探针(V7 已过时,零引用)
```

## 🟡 软砍候选(判定=冻结但 spent · 人工确认后可清 · 8 项)

`ops/_verify_retrieve_gpu.py`(T4 已关·仅差 1 天未过 30d 线)· `ops/v0.9_to_v14_adapter_patch.py` · `ops/patch_v14_recall_poi_boost.py` · `ops/patch_v14_recall_poi_candidate.py` · `ops/smoke_keepalive_fix.py` · `ops/smoke_long_idle.py` · `scripts/gmail_oauth_init.py` · `scripts/patch_evermembench_persist.py` —— 均已应用/已过,仅剩零星文档引用。

## ⚠️ 灰色待确认(4 个 outreach cron · 越界)

`arxiv_discovery_cron` / `engagement_followup_cron` / `gmail_inbound_monitor_cron` / `outreach_metrics` —— 有 `.sh` 挂载按规则判活,但属 **outreach/paper 线,超 SSOT compass turf**(记忆/探针/feishu)。**建议查线上 crontab:已停跑则砍,在跑则移交对应 turf。**

## 📦 文档归档建议(非砍)

- **16 个 `_OUTBOUND_*.md` 堆 repo 根** = 已投递的一次性广播,堆积本身就是 D 陷阱。14 天内判活,但建议整体挪 `docs/archive/`(含今日新增 5 个)。
- **10 个 `paper/SPEC_*.md`**:1 活(PoI)/ 9 冻结(多为 05-21 的 v2.0 新架构 + paper 规格,SSOT §七 已冻结"新架构"线)。4 个 untracked 草案(GBRAIN/DECLARATION/FILLER32K/MARKETING)未提交,可评估是否弃。

## 建议砍法(留用户拍)

1. **死 18 项**:直接删(spent 一次性件,git 历史可追,无引用无风险)。
2. **软砍 8 项**:确认已应用后删。
3. **outreach 4 cron**:查 crontab 决定砍/移交。
4. **_OUTBOUND 16 个**:批量挪 `docs/archive/`。

—— compass · 2026-07-07 · 全 grounded,砍板留你。
