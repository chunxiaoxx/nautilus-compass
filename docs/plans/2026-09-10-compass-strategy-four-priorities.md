# COMPASS 战略四优先级(2026-09-10 用户拍板)

> 依据:七算子审计(9/7)+ 仲裁栈五层设计 + 三实验读数 + flywheel P3 纲领(9/9)。
> 定位:不做第七个记忆产品,做「决定什么是事实」的仲裁层。
> 对句:Aegis 仲裁动作,我们仲裁记忆——以及数据的价值。
> 协同拼图:flywheel 判数据(判定器生产端),compass 判「判数据这件事可信」(验证制度端)。拼图非重叠。

## 四优先级(序=执行序)

### P1 · 9/15 记忆门三件套合入(RSI 试验环第一闭)

- 材料:daemon 仓 `feat/memory-gate-trio` 全实现 + 15 单测绿 + J4 基线 GREEN + 预注册判据落档(`2026-09-09-rsi-trial1-preregistered.md`)
- 动作:合入 → 受控重启 → `ops/regression_gate.py` 对照基线 → J1-J3 读数回填预注册档 → 非实现者复算
- 验收:J1-J4 四判据全读数 + 复算通过

### P2 · batch002 全流程(第一个对外样本)

- 内容:flywheel 200 条掌形批次按 VerifyPack v0.2 走 build → verify → receipt → check 全链
- 前置:flywheel 200 条批次就绪(其排 Q4);9/10 22:00 收口会定「4 门判定器(数据侧)× CLI 复算(验证方)」拼管线正本
- 验收(外部采信):包 + 签名回执可被自家四框之外的人独立验签

### P3 · 判据库 v0(数据有效性判定语言的第一个公开正本)

- 内容:flywheel 五族判据(D 形式/C/L 规则、X 语义、U 效用)+ G1 几何卫生规则装进 compass 判据库
- 起步:9/10 22:00 收口后;判据必须可执行(不是文档列表),每条带实测背书引用
- 验收:判据库被 batch002 实际调用(闭环使用才算立住)

### P4 · 论文线:盲区定理 + 双速异构写进 paper2

- 内容:G1 盲区随分布换形(特异度 1.0,双数据集双证)+ 双速异构验证原则 → paper2 判官盲区融合版
- 时点:flywheel G1 报告归档(9/28 前)后成稿
- 口径红线:P01 不宣称几何超判官;对外只引 S2a_hn(near-miss 判官放行 45.5%)

## 三条发展线(背景框架)

1. **介质线**(现在,基本盘):本地优先记忆 + 检索 0.890 P@1 + 写侧零 LLM——同时是仲裁线自己的被仲裁对象
2. **仲裁线**(差异化主体):写侧门(三件套)× 声称验证(VerifyPack)× 判据语言(判据库)
3. **共识线**(理论押注,2027 双假设裁决):忘=共识函数——记忆存亡由被独立复算的支撑度决定(记忆的 PoS);RSI 试验环=先仲裁自己

## 不做

动作门(Aegis 已占)/ 代币化 / 大爆炸重写

## 反剧场主尺

每阶段验收不看件数,看**第一个自家四框之外的复算请求何时出现**。
候选:Trajko(quant 场景,Hivemind 架构师,已两轮通信)/ dev.to 判分卫生学文章触达人群。

## 协同机制备忘

- 分工红线:flywheel 管生产+自证,compass 管独立验证+判据;VerifyPack 宪法边界(验证方不作客户验收、不作结算决定)
- 已闭:batch001 复算+签名回执(6/7 agree+1 活文件实锤);VerifyPack v0.2 回执互认
- 进行:今晚 22:00 收口(X1 金标过线后 17.5% 错配候选入包,双方已同意此门槛);G1 几何判官进判据库当第一个外部锚
- 结算:verified 标签进 V5 训练数据选择 → 外部数据商采信=分水岭;compass 只出可验签判定,不碰数据不碰训练
