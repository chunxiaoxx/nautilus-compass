# nautilus-compass v1.0 release checklist

Tracking surface for the 1.0.0-rc1 → 1.0.0 promotion. Tick items honestly · do
not promote to 1.0.0 until every gate under "Eval gates" is green.

## Code complete

- [x] Merkle hash chain module (`merkle_chain.py` · 349 LoC · 6/6 smoke)
- [x] Merkle integrated into `session_writer._finalize_chain` · fail-soft hook
- [x] `compass_verify.py` CLI · `--all` / `--project` modes
- [x] MCP A2A server (`mcp_server.py` · 20 KB · JSON-RPC 2.0 over stdio · 7 tools)
- [x] `ZMM_TEMPORAL` env gate for temporal-reasoning A/B
- [x] Absolute time anchor in memory writes (`time_anchor.py`)
- [x] Temporal prompt + timeline scratch-pad in eval
- [x] ssu utterance-pair retrieval · `ZMM_SSU_UTTERANCE` gated
- [x] Self-consistency majority vote · `ZMM_VOTE` gated
- [x] Hybrid BM25 + dense RRF · `ZMM_HYBRID` gated
- [x] Context top-5 → top-10 · `ZMM_TOPK` gated
- [ ] v1 vs v2 driver ablation (Task #20 · EverMemBench rerun blocked behind gate48)

## Eval gates

- [x] subset-48 A/C/D delta ≥ 0 vs baseline (Task #29 · done · Run A 31.2% ·
  Run C 31.2% · Run D 35.4% · **D > A by +4.2** validates ssu; vote=3 = 0 gain)
- [~] full-500 V4-pro rerun (Task #27 · running · gate48 sign-off pass · full-500
  started 02:09 local on T4 · pid 1363898 · ETA ~7h · watcher auto-triggered)
- [x] cross-judge replication result ≥ Gemini-pro judge - 3 pts (Task #26 · done ·
  Claude judge showed +2 pts agreement vs Gemini-pro · within 3-pt band)

## Docs

- [x] `CHANGELOG.md` · 1.0.0-rc1 entry
- [x] `README.md` · feature bullets referencing Merkle + MCP
- [x] `docs/v1.0-checklist.md` (this file)
- [x] MCP server usage doc · `docs/mcp-usage.md`

## Version

- [x] `pyproject.toml` · `1.0.0-rc1`
- [x] `package.json` · `1.0.0-rc1`
- [x] `mcp_server.py` · `SERVER_VERSION = "1.0.0-rc1"`

## Release

- [ ] Tag `v1.0.0-rc1` on main (release automation pre-staged · Task #41).
  `.github/workflows/release.yml` triggers on `v1.*` tags, runs the full
  78-test pytest sweep as a release gate, extracts release notes from
  CHANGELOG via `scripts/release_notes_extract.py`, and attaches
  CHANGELOG + checklist + mcp-usage. rc-tags auto-marked as prerelease.
  Guarded by 5 unit tests against CHANGELOG format drift. A single
  `git tag v1.0.0-rc1 && git push --tags` is all that's needed when the
  eval gates clear.
- [ ] GitHub release draft · auto-built by `release.yml` once the tag
  exists. Manual step: slice the paper PDF into the release assets after
  paper2 freeze.
- [ ] Promote to `1.0.0` once all "Eval gates" checked

## Quality gates

- [x] MCP + A2A protocol pytest suite (Task #37 · done · 19 tests · 0.11s ·
  no daemon dep · wired into CI as `v10-protocol-tests` job). Covers:
  initialize / tools/list / tools/call happy path + daemon-offline graceful
  fail + A2A envelope validation + DISCOVER_CAPABILITIES zero-side-effect
  + all 5 A2A message types delegate to the right MCP tool.
- [x] Merkle chain pytest suite (Task #38 · done · 12 tests · 0.15s ·
  `tests/test_merkle_chain.py` · joined into `v10-protocol-tests`). Covers
  empty-dir init, clean init+verify, edit-detects-tampered, delete-detects-
  missing, new-file-pre-rebaseline, rebaseline-adopts-new, corrupt-chain-
  json-does-not-crash, deterministic head, filename-ordered (not mtime-
  ordered), bytes-differ-head-differs, mtime-only-touch-not-tampering,
  chain.json shape contract. Landed a real fix: `_read_chain` used to
  crash on corrupted JSON — now graceful.
- [x] MCP non-Claude client onboarding (Task #40 · done). `docs/mcp-usage.md`
  grew a raw JSON-RPC 3-step handshake example + shipped
  `scripts/mcp_smoke_rpc.py` (subprocess stdio, utf-8 forced, exit codes
  0/1/2). Verified against live daemon: initialize → tools/list (7 tools)
  → `--tool recall` returns real hits. Unblocks Hermes / OpenClaw
  integration without needing to read the MCP spec or our source.

## Notes

- [x] Gate48 Run A finished 2026-05-08 01:13 local · gemini-2.5-flash subject ·
  overall 31.2% (prior deepseek-v3.2 baseline = 56.25%) · flash is systematically
  ~25 pts weaker. The **delta shape** across A/C/D is what matters for the paper.
- [x] Gate48 Run C finished 2026-05-08 02:04 local · gemini-2.5-flash +
  ZMM_VOTE=3 · overall **31.2% (same as Run A · 0 gain)** · wall 2395s vs
  Run A 917s (**2.6× cost**). Candidate negative finding for paper2 §4.6:
  self-consistency on weak subject models amplifies noise, not signal.
- [x] Gate48 Run D (+ZMM_SSU_UTTERANCE=1) · overall 35.4% · ssu 37.5% (+12.5
  vs baseline) · ssp 37.5% (+12.5) · **no other per-type regression**. ssu
  utterance-pair retrieval validated on weak subject model (gemini-2.5-flash).
- [x] **Gate48 three-run headline**: Run A (baseline) 31.2% · Run C (vote=3)
  31.2% (**0 gain, 2.6× cost · surprising finding: low-temperature vote
  collapses on weak models**) · Run D (+ssu) 35.4% (+4.2 overall, ssu target
  segment +12.5). Clean ablation signal for paper2 §4 v1.0-rc1 row.

  Root cause for Run C = Run A byte-identity: eval calls gemini-2.5-flash
  at temperature=0.1. flash behaves near-deterministically at that temp;
  3 samples collapse to identical output, majority-vote degenerates to
  single-call with 3× cost. **Future work:** re-run gate48 Run C' at
  temp=0.7 to measure whether higher temperature recovers vote gain. Not
  a v1.0 blocker · log as Tier 3 item for paper2 revision.
- [x] Full-500 data point on deepseek-v4-pro subject + v4-flash judge (run
  1778075052): overall 56.4%, per-type ssa 87.5%, ssp 66.7%, ssu 58.6%, ku
  56.4%, ms 52.6%, temporal 43.6%. Against v0.8 (deepseek-v3.2 + 5-stage): -0.2 pts
  overall. v3.2 → v4-pro subject upgrade does **not** improve this benchmark;
  new capabilities (Merkle / MCP / temporal prompt) are the v1.0 story.
- [ ] Full-500 gemini-2.5-pro + vote=3 + ssu + topk=10 + temporal: watcher
  armed (pid 1353956) · fires after gate48.DONE · ETA +85min.
- [x] `1.0.0-rc1` CHANGELOG ships with the MCP surface as **preview**, with
  a concrete path to **stable**: `scripts/mcp_smoke_rpc.py` acts as a
  reference external client and has exercised the full 3-step handshake
  plus `recall` and `drift_check` against the live daemon (7 tools listed,
  recall returns 3 hits at score 0.432, drift_check flags a prompt-injection
  sample with alert=True cos=0.636 against negative anchors). Stable
  promotion blocker: one **third-party** client (Hermes or OpenClaw, not
  our own smoke script) verifying the same two tools over a full session.
- Task #20 · EverMemBench v1 → v2 driver ablation plan (ready to run once T4
  is free · `scripts/evermembench_bge.py` is now fully env-gated):
  ```
  # Baseline (v1, paper Table 4 protocol — expect 41.0%)
  ANSWER_MODEL=deepseek-v4-flash EM_REWRITE=0 EM_DAY_BUCKET=0 python scripts/evermembench_bge.py
  # +subject upgrade only (v4-pro contribution)
  ANSWER_MODEL=deepseek-v4-pro   EM_REWRITE=0 EM_DAY_BUCKET=0 python scripts/evermembench_bge.py
  # +multi-angle rewrite only
  ANSWER_MODEL=deepseek-v4-flash EM_REWRITE=1 EM_DAY_BUCKET=0 python scripts/evermembench_bge.py
  # +day-bucket only
  ANSWER_MODEL=deepseek-v4-flash EM_REWRITE=0 EM_DAY_BUCKET=1 python scripts/evermembench_bge.py
  # v2 current default (expect 44.4%)
  ANSWER_MODEL=deepseek-v4-pro   EM_REWRITE=1 EM_DAY_BUCKET=1 python scripts/evermembench_bge.py
  ```
  4 × ~30 min on T4 (v2 already measured) = ~2h incremental · each knob's
  marginal contribution attributed by subtracting from v1 baseline.
