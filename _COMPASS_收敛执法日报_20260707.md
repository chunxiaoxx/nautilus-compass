# 🧭 compass 收敛执法日报 · 2026-07-07

> 角色①收敛执法 + ②合约管家。**全部数字 = compass 独立探针读生产 DB / 飞书真值,非自报。**
> 守则:自报不算,探针才算。🚩 = 自报与探针不符(亮牌);⚠️ = 口径歧义需澄清;✅ = 符。
> 探针:`ops/economy_liveness_probe.py` + `ssh cloud`(compass_sub 只读 DB · 白名单 IP 读飞书)。

---

## 一、5 记分牌 · 探针实测 vs SSOT §二 自报

| 记分牌 | SSOT §二 自报 | compass 探针实测(grounded) | 出处 | 判定 |
|---|---|---|---|---|
| **income** | 504 | agent_survival.total_income(9000009)=**504**(GREEN,权威)· verdict-derived=**526**(9 铸币;审计 40min 内由 cache_lru 自动 +11:515→526) | `probe income_ground_truth` + `fde_verdicts WHERE external_verified∧overall_pass` | ✅ 符 |
| **自治率** | 30%(3/10) | **4** 条 `autonomous=true` 铸币 verdict:cache_lru / jssp_la_15x15 / jssp_la_15x10 / bin_packing_ffd_v2,均 7/7 18:44–22:58 被 daemon 自动验证铸币 | `fde_verdicts.artifacts->>'autonomous'` | 🚩 **自报低估**:实测≥4(SSOT 写 3,审计期间 cache_lru 又 +1)· 分母 /10 为 V5 内部量,不可独核 |
| **settle** | 0/3617 | a2a 结算路由 `send_to_agent` **218 调用 / 0 成功**、`nautilus_claim_bounty` **16/0**(全 404)→ 自循环 settle=**0 属实**;但 mint-settle 已动(income 188→504) | `agent_tool_calls` 近 48h | ✅ 0 属实 · ⚠️ 分母 3617=agent_actions,compass_sub 无 grant **不可读(GAP)** |
| **verdict(外部验证)** | 10 | external_verified=**11**(9 pass 铸币 + TSP 97.63 overall_pass=False 未铸 + gate-probe 50 rejected) | `fde_verdicts COUNT FILTER` | 🚩 轻微 drift:实测 11,自报 10 |
| **交付** | 0/11 | 飞书 v06 行数:学员 16 / 派活 2 / 交付总表 29 / 审计 12;但 FDE 自评"招募 0 真用·1 道自造模拟题"→ **真甲方交付 = 0/11** | feishu RGDK base(cloud 白名单读) | ⚠️ **行数 ≠ 真交付**:交付表 29 行 vs 真交付 0/11(行多为 seed/模拟) |

**附 · engine_cycle(soul 自循环):** `STALE 34.2d`(last 2026-06-03,24h 0 cycle)= **soul 引擎自循环停摆**(非健康,诚实 surface)。⚠️ 与 producer 无关:producer **prime-001 近 24h 6045 calls / 5711 ok / last 23:39** = 活跳。"cron 0 执行"是误导——循环猛跑,断的是 cycle 落账,不是进程。

---

## 二、FDE 培训链 4 表 · 探针实测 vs FDE 自报

> base app_token = `RGDKbrtZ1aMcEOsZ2GcczeFknNg`(feishu_admin.py 默认 `FDE_RGDK_BASE`)。
> ⚠️ 家宽 IP(1.192.65.70)被飞书 app IP 白名单拒(code 99991401),经 **cloud 白名单 IP(43.160.239.61)** 读通。

| 表 | table_id | FDE 自报 | 探针实测行数 | 判定 |
|---|---|---|---|---|
| v06_行业专家(学员) | tblQChALfBACGLYB | 16 | **16** | ✅ 符 |
| v06_派活 ★核心 | tblfPoDrZzKHpU36 | (无报) | **2** | — 派活极少(2 行)· 印证真派单尚未起量 |
| v06_交付总表 | tblwz7PQZrZW3UzO | 29 | **29** | ✅ 行数符 · ⚠️ 非真甲方交付(见记分牌⑤) |
| v06_审计日志 | tblqX49MQooUZjKH | 0 | **12** | 🚩 **不符**:自报 0,实测 12 行 |

**亮牌解读**:审计日志 12 行 ≠ 自报 0——要么自报陈旧,要么有 12 条操作未被 FDE 纳入台账。派活仅 2 行 + 真交付 0/11,佐证"培训链已建表但真人生产未起量"(与 SSOT §四 FDE"陪跑第一批真人培训"仍未发生一致)。

---

## 三、7/9 两条 V5 合约验收(读 DB/git 核实,不闭标红)

### ① `cnt_compass_v5_fake_success_produce`(due 7/9)→ 🟡 **部分兑现 · 不标 consumed**
- ✅ guard 已 commit:V5 `a3795c2 fix(fde): 假成功守卫 · 空/空白 produce 不上报不算 processed 成功`。
- ✅ **下游脱 0 已 grounded**:4 条 `autonomous=true` verdict 7/7 被 auto-verify daemon 自动验证铸币(income +316:188→504)= 合约意图"自治 producer 自产入账"事实发生。
- 🔴 **未竟**:
  - (a) 字面判据"claim_id=None 是否还返回 success"**无法从账本直证** —— `agent_tool_calls.output_summary` 截断为 `"ok"`,读不到 claim_id;`selector:fde_claim_produce` 仍 16686 success、output 全 "ok"、last 16:54。需 V5 出示 guard 生效证据(一条 claim_id=None 返回非 success 的调用)。
  - (b) `settle 从 0 动` 的 a2a 路径仍 **404**(send_to_agent 218/0)——那是另一条 **platform 合约** `cnt_compass_platform_settle_routes_404`,非本合约。
- 判据:自治脱 0 兑现 → 但 hollow-guard 直证 + a2a settle 未闭 → **不标 consumed**。

### ② `cnt_compass_v5_evaluate_artifacts_fix`(due 7/9)→ 🔴 **未兑现 · 标红**
- 🔴 V5 git log **无** evaluate/artifacts 相关 commit(最近 `ef7956c docs`);`fde_capsule/genopt/tasks/*/evaluate.py` 仍**只写 metrics.json**(grep `_Tee`/`_write_artifacts_index`/`artifacts.json` 全 0 命中)。
- ✅ compass 侧已交付:`vtf/fde-toolbox/fix_evaluate_artifacts.py`(commit 4273f83)+ 修 2 交付题重打包已发用户。
- 缺口:V5 未 batch-apply 全 11 题 + 未修 `gpt55_local_runner.py`/出题模板(否则新题复发)。
- 判据:**未兑现,标红**。买方(王老师)反馈的清单产物缺失(eval.stdout/stderr.txt)在生产工厂层仍存在。

---

## 四、DONE 判据自检

- ✅ ① 日报出:5 记分牌 + 4 表**全有探针实测值**(非自报)。
- ✅ ② 两条合约各有 grounded 验收结论(①部分兑现不 consumed · ②未兑现标红)。
- ✅ ③ 飞书 base app_token 已补:`RGDKbrtZ1aMcEOsZ2GcczeFknNg`。

## 五、待用户拍(compass 不越界·亮牌上报)

1. **记分牌校正通报**:自治率(3→≥4)、verdict(10→11)对 platform 更新 SSOT §二?(compass 该亮牌,发通报待点头。)
2. **飞书 IP 白名单**:compass 家宽 IP 被拒,现依赖 cloud 中转读飞书。是否把 compass IP 加入 app 白名单(飞书管理后台·用户操作),否则每次读飞书须过 cloud。
3. **交付口径**:交付总表 29 行 vs 真甲方交付 0/11——记分牌"交付"该以行数还是真交付计?(建议真交付,行数标注 seed。)

---
*探针脚本 + SQL 全可复现。原始查询在 scratchpad/audit*.py。关联 [[LOOP_STATE_SSOT]] · [[reference_compass_db_verdict_income_read_paths_20260707]]。*
