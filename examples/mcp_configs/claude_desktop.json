{
  "_doc": "Paste this into ~/Library/Application Support/Claude/claude_desktop_config.json (macOS) or %APPDATA%/Claude/claude_desktop_config.json (Windows). Restart Claude Desktop after.",
  "mcpServers": {
    "compass": {
      "command": "compass-mcp",
      "env": {
        "COMPASS_USER_ID": "u_REPLACE_ME",
        "COMPASS_AGENT_TYPE": "claude-desktop",
        "COMPASS_BASE_URL": "https://compass.nautilus.social"
      }
    }
  }
}
